resources:
  jobs:
    finhive_ingest_yahoo:
      name: finhive_ingest_yahoo
      schedule:
        quartz_cron_expression: 0 30 16 ? * MON-FRI
        timezone_id: America/New_York
        pause_status: UNPAUSED
      tasks:
        - task_key: ingest_yahoo
          notebook_task:
            notebook_path: /Workspace/Users/facumazzola@hotmail.com/finhive-2026/notebooks/data_ingestion/yahoo/orchestrator
            base_parameters:
              job_name: "{{job.name}}"
            source: WORKSPACE
          environment_key: ingestion
      queue:
        enabled: true
      environments:
        - environment_key: ingestion
          spec:
            client: "3"
            dependencies:
              - yfinance
      performance_target: PERFORMANCE_OPTIMIZED
